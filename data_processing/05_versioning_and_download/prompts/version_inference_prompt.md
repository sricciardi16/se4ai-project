Based on the Core Essential Public API Contract you just defined above, what is the indicative Major.Minor version (e.g., "2.4", "3.0") of this library that you are referencing? 

I am interested in the version that covers these main functionalities, ignoring backward compatibility.

**CRITICAL INSTRUCTIONS:**
Output strictly a valid JSON object enclosed in markdown backticks (```json ... ```).

**REQUIRED JSON SCHEMA:**

```json
{
  "target_version": "string"
}
```