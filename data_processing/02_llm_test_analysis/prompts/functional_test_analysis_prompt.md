I am now providing you with the original `functional_test.py` used to evaluate this library. 

Go straight into analyzing **each individual test function** found in the code.

For EVERY test function, provide the following breakdown:

### `[Test Function Name]`
* **What it tests:** A clear description of the specific feature, behavior, or edge case being evaluated.
* **Black-Box Status:** Is it strictly Black-Box? If it uses internal APIs, private variables (`_`), or non-public structures, point them out.
* **Conversion Feasibility:** If it is White-Box, explain briefly how we could test this *same behavior* using ONLY the public API contract. If impossible, state why.
* **Criticism & Salvage:** Is the test brittle, poorly written, or lacking meaningful assertions? Conversely, if it contains a clever edge case or specific data input we should keep, extract the 1-2 crucial lines of code.

Here is the original functional test code:
```python
[INSERT TEST CODE HERE]
```