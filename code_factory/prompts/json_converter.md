Your task is to extract information from a text input and format it into a valid JSON array.

# Input

You will be provided with:
1. **Required Schema:** The exact JSON structure you must follow.
2. **Text Input:** The raw text containing the information to be extracted.

# Output

Output valid JSON that strictly matches the provided schema. Ensure all strings are properly escaped. Wrap the JSON in a single Markdown code block.

```json
[JSON output]
```
